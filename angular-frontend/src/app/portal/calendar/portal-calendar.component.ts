import { Component } from '@angular/core';

@Component({
  selector: 'app-portal-calendar',
  template: `
    <div>
      <h3 class="text-2xl font-bold mb-4">Calendar</h3>
      <p>Calendar placeholder (use PrimeNG Calendar in full migration).</p>
    </div>
  `
})
export class PortalCalendarComponent {}
